import { CircleButton } from "../components/Button";

const Header = () => {
    return (
        <header className="flex items-center justify-between text-white">
            <CircleButton>AA</CircleButton>
            <CircleButton>AA</CircleButton>
        </header>
    );
};

export default Header;